{
  "name": "kluster-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive reduced-order simulator for the magnetic game Kluster: dipoles, dry friction, tipping, capture cascades, moving boundaries, and sequential strategy.",
  "scripts": {
    "dev": "npx serve .",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check js/physics-core.js && node --check js/world.js && node --check js/physics.js && node --check js/pathfinding.js && node --check js/presets.js && node --check js/renderer.js && node --check js/ui.js && node --check js/app-base.js && node --check js/app-runtime.js && node --check js/main.js"
  },
  "engines": {
    "node": ">=20"
  }
}
